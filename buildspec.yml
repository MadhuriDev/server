version: 0.1
phases:
  install:
    commands:
      - printenv
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - mkdir -p test-app
artifacts:
  files:
    - test-app*/**
  discard-paths: yes
